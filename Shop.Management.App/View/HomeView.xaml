<UserControl x:Class="Shop.Management.App.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Shop.Management.App.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
               DataContext="{Binding Source={StaticResource MainViewModelLocator},  Path=HomeViewModel}">
    <Grid>
        <Button Margin="5" Command="{Binding LoadSampleDataCommand}" Content="Load Data"></Button>
    </Grid>
</UserControl>
